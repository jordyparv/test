loadSwitchService.prototype.upsertINTOSwitchAttachment =  function (
  data,
  callback
) {
  if (data == undefined || data.length == 0)
    return console.error("No data found for insert SwitchAttachment");
  let query = queryFactory.getQuery("syncSwitchAttachment");

  let errors = [];

  data.forEach((_data) => {

    oraUtil.execute(query, _data, (err, results) => {
      if (err) {
        errors.push(err);
      } else {
        logger.info("switch attachment data sync successfully.");
        //  return callback(null, results);
      }
    });
})
  if (errors.length !== 0) {
   callback(errors);
  }
else
callback(null,[])
};
