-- Get trigger details
SELECT TRIGGER_NAME, TRIGGER_TYPE, TRIGGERING_EVENT, TABLE_NAME
FROM USER_TRIGGERS
WHERE TRIGGER_NAME = 'SWITCH_DETAILS_AUDIT_LOG';

-- Get trigger code
SELECT TEXT
FROM USER_SOURCE
WHERE NAME = 'SWITCH_DETAILS_AUDIT_LOG'
ORDER BY LINE;
