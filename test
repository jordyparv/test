const rateLimit = require('axios-rate-limit');

const http = rateLimit(axios.create(), { maxRequests: 1, perMilliseconds: 1000 });

console.error(`Error downloading file: ${url} - Message: ${error.message}`);
