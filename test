  config.get.mockImplementation((key) => {
    const configObj = {
      reverseSync: {
        useReverseSync: false  // Manipulating value for the test
      }
    };

    return key.split('.').reduce((acc, part) => acc && acc[part], configObj);
  });
