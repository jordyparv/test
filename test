newRows?.forEach(newRow => {
  const exists = rows.some(existingRow =>
    existingRow.element_name === newRow.element_name &&
    existingRow.vendor === newRow.vendor &&
    existingRow.sub_element_name === newRow.sub_element_name
  );
  
  // If the row doesn't exist in the array, push it
  if (!exists) {
    rows.push(newRow);
  }
});
