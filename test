  const newRows = formedState?.sub_element_name?.map(subElement => ({
        element_name: formedState?.element_name,
        vendor: formedState?.vendor,
        sub_element_name: subElement,
        elementId:subElementOptions?.find(item=>item.ELEMENT_DETAILS===subElement)?.ELEMENT_ID
      }));
