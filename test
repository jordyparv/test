 exceptionFactory: (errors) => {
        // You can customize how errors are formatted here
        const messages = errors.map(err => `${err.property} has wrong value ${err.value}, ${Object.values(err.constraints).join(', ')}`).join(', ');
        return new Error(messages);
      },
